import React, { Component } from 'react';
import Api from '../api';
class Useradd extends Component{
    render (){
        return(
            <div>
                用户添加
            </div>
        )
    }
}
export default Useradd;